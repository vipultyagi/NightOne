import numpy

['Fizz'*(not i%3) + 'Buzz'*(not i%5) or i for i in range(1, 100)]

def fizzbuzz(Jim_Crystal):
    str = ['Fizz'*(not i%3) + 'Buzz'*(not i%5) or i for i in Jim_Crystal]
    return str;

fizzbuzz([1234567890,20,30])
